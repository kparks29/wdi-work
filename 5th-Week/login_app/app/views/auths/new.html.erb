<div class="login">
	<h1>Log In</h1>
	<%= form_for @user, {url: auth_path, method:"post"} do |u|%>
	<%= u.text_field :name, :placeholder => "Your username" %><br>
	<%= u.password_field :password, :placeholder => "Your password" %><br>
	<%= u.submit "Log In" %>
	<%end%>
</div>